service: proompting-frontend
image: proompting-frontend

servers:
  web:
    - game.timwillebrands.nl

proxy:
  host: game.timwillebrands.nl
  ssl: false
  app_port: 3000
  healthcheck:
    path: /

ssh:
  user: root
  keys:
    - ~/.ssh/kamal_ed25519

registry:
  server: localhost:5555

builder:
  arch: amd64
  dockerfile: frontend/Dockerfile
  context: frontend

